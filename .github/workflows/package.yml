name: Build and validate Debian package

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Debian packaging tools
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y lintian

      - name: Build package
        run: dpkg-deb --build --root-owner-group . ../ananicy-debian.deb

      - name: Validate package metadata
        run: |
          dpkg-deb --info ../ananicy-debian.deb
          dpkg-deb --contents ../ananicy-debian.deb

      - name: Run lintian
        run: lintian --no-tag-display-limit ../ananicy-debian.deb
